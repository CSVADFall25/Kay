### temp notes


have to provide disk access to VSCode/terminal in Settins > Privacy and Security > Full Disk Access
or they can't access the chat.db file from iMessages


### requirements

Spotify Developer API to query spotify for track information (to get artists for a linked track)  
Google Developer/Cloud API to query youtube v3 API for youtube title information from linked videos

pip install:

- python3 -m pip install nltk
- python3 -m pip install spacy
- python3 -m spacy download en_core_web_sm